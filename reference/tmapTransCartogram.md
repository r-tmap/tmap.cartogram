# Cartogram transformation

Cartogram transformation

## Usage

``` r
tmapTransCartogram(shpTM, size, ord__, plot.order, args, scale)
```

## Arguments

- shpTM, size, ord\_\_, plot.order, args, scale:

  tmap internals

## Value

list of two. The first is a spatial object, the second the tmap ID codes
